#include <iostream>
#include <algorithm>
using namespace std;

int main(void)
{
    int n;
    std::cin >> n;

    while (n--)
    {
        int in_i, in_j, k;
        cin >> in_i >> in_j >> k;

        for (int i = 1; i <= in_i; i++)
        {
            for (int j = 1; j <= in_j; j++)
            {
                        }
        }
    }

    return 0;
}